#!/bin/bash

sqlite3 sjsmusic.db -column -header 'select f.momefid, f.name, count(p.id) as payments from family as f left join payment as p on f.momefid = p.momefid where f.session = (select id from session where active is not null) group by f.momefid order by count(p.id) desc;'
